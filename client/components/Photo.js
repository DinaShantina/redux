import React from "react";

const Photo = React.createClass({
  render() {
    return (
      <div>
        <figure className="grid-figure">I am a photo</figure>
      </div>
    );
  }
});

export default Photo;
